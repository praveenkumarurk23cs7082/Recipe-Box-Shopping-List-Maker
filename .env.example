# Local dev default (SQLite, zero setup) — copy this file to .env and adjust if needed.
DATABASE_URL=sqlite:///./recipe_box.db

# When #7 has Cloud SQL ready, DATABASE_URL for the deployed app will look like:
# DATABASE_URL=postgresql://USER:PASSWORD@/DBNAME?host=/cloudsql/PROJECT:REGION:INSTANCE
# (set as an actual env var / Secret Manager value on Cloud Run — don't commit real creds)
