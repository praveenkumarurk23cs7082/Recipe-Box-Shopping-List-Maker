import RecipeDetailPage from "./RecipeDetailPage.jsx";

/**
 * Integration metadata for Role #6's router. This isolated feature does not
 * provide shared navigation or a global router.
 */
export const recipeDetailRoute = {
  path: "/recipes/:recipeId",
  Component: RecipeDetailPage
};
